import { FileUploadInterceptorInterceptor } from './file-upload.interceptor.interceptor';

describe('FileUploadInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new FileUploadInterceptorInterceptor()).toBeDefined();
  });
});
